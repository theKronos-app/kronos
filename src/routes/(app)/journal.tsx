import { type JSX, type ParentComponent } from "solid-js";

type JournalProps = {};

export default function Journal(
  props: JournalProps & ParentComponent,
): JSX.Element {
  return <div>Journal</div>;
}
